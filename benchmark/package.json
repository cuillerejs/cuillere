{
  "name": "@cuillere/benchmark",
  "version": "0.6.0",
  "main": "src/benchmark.js",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@cuillere/core": "^0.6.6",
    "benny": "^3.6.14"
  },
  "scripts": {
    "start": "node --harmony --expose-gc .",
    "prof": "node --harmony --expose-gc --prof . && node --prof-process --preprocess -j isolate*.log | flamebearer",
    "debug": "node --harmony --expose-gc --inspect-brk ."
  }
}
