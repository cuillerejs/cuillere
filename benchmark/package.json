{
  "name": "@cuillere/benchmark",
  "version": "0.0.0",
  "main": "src/benchmark.js",
  "license": "Apache-2.0",
  "private": true,
  "devDependencies": {
    "@cuillere/core": "^0.8.0",
    "benny": "^3.6.14"
  },
  "scripts": {
    "start": "node --harmony .",
    "prof": "node --harmony --prof . && node --prof-process --preprocess -j isolate*.log | flamebearer",
    "debug": "node --harmony --inspect-brk ."
  }
}
