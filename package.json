{
  "private": true,
  "scripts": {
    "build": "cd core && yarn build && cd ../channels && yarn build && cd ../mariadb & yarn build && cd ../postgres && yarn build && cd ../server && yarn build && cd ../server-postgres && yarn build "
  },
  "workspaces": [
    "channels",
    "core",
    "mariadb",
    "postgres",
    "server",
    "server-example",
    "server-postgres",
    "server-postgres-example"
  ],
  "devDependencies": {
    "@tsconfig/node12": "^1.0.7",
    "@tsconfig/node14": "^1.0.0",
    "@types/jest": "^26.0.14",
    "@types/pg": "^7.14.7",
    "@typescript-eslint/eslint-plugin": "^4.8.1",
    "@typescript-eslint/parser": "^4.8.1",
    "eslint": "^7.13.0",
    "eslint-config-airbnb-base": "^14.2.1",
    "eslint-plugin-import": "^2.22.1",
    "eslint-plugin-jest": "^24.1.3",
    "esm": "^3.2.25",
    "jest": "^26.6.3",
    "ts-jest": "^26.4.4",
    "typescript": "^4.0.5"
  },
  "dependencies": {
    "graphql": "~14",
    "mariadb": "^2.5.1",
    "pg": "^8.5.1"
  }
}
