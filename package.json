{
  "private": true,
  "scripts": {
    "build:all": "yarn workspaces foreach --no-private --all --topological-dev --verbose --parallel run build:all",
    "publish:all": "yarn workspaces foreach --no-private --all --topological-dev --verbose publish --no-git-tag-version --non-interactive",
    "test": "yarn workspaces foreach --no-private --all --topological-dev --verbose --parallel run test"
  },
  "workspaces": [
    "channels",
    "core",
    "mariadb",
    "postgres",
    "server",
    "server-example",
    "server-plugin",
    "server-postgres",
    "server-postgres-example"
  ],
  "devDependencies": {
    "@tsconfig/node12": "^1.0.9",
    "@tsconfig/node14": "^1.0.1",
    "@tsconfig/node16": "^1.0.2",
    "@types/jest": "^27.0.2",
    "@types/pg": "^8.6.1",
    "@typescript-eslint/eslint-plugin": "^4.28.0",
    "@typescript-eslint/parser": "^4.28.0",
    "eslint": "^7.32.0",
    "eslint-config-airbnb-base": "^14.2.1",
    "eslint-plugin-import": "^2.25.2",
    "eslint-plugin-jest": "^24.7.0",
    "esm": "^3.2.25",
    "jest": "^27.3.0",
    "ts-jest": "^27.0.7",
    "typescript": "^4.4.4"
  },
  "dependencies": {
    "graphql": "~15.6.1",
    "mariadb": "^2.5.4",
    "pg": "^8.7.1"
  },
  "packageManager": "yarn@3.0.2"
}
