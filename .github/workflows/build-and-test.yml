name: Build and test
on: [push]

jobs:
  build:
    name: Build
    runs-on: ubuntu-20.04
    steps:
      - name: Install node v14
        run: nvm install 14
      - name: Show node version
        run: node --version